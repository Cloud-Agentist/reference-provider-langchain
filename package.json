{
  "name": "reference-provider-langchain",
  "version": "0.1.0",
  "private": true,
  "description": "Reference reasoning provider using LangChain.js — baseline comparison against Dreasen in the reasoning-gym.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@langchain/core": "^0.3.0",
    "@langchain/openai": "^0.3.0",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
